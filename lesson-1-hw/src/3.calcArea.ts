const calculateRectangularArea = (length: number, width: number): void => {
  const area: number = length * width
  console.log(`The area is ${area} m2`)
}

calculateRectangularArea(5, 7)
calculateRectangularArea(6, 8)
